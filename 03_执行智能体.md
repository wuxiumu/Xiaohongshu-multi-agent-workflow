# 03 · 执行智能体 Executor

> 出成品。把 02 的选题按三轨分别产出可发布的小红书笔记:教程步骤、skill 脚本、PPT 成品。

## 1. 定位

- 输入:`data/judged.json`(02 产出)
- 输出:`data/produced.json` —— 三轨分桶的笔记成品 + 配图 + PPT 模板文件
- 不做:不做选题决策、不抓外部数据

## 2. 三轨差异化产出

三轨笔记结构、配图、产出物完全不同,执行器内部按 `track` 分派不同子流程。

### 2.1 教程轨 `tutorial`

**笔记字段**
- `title`:标题(≤20 字,带数字/场景/痛点,如"打工人哭了!AI一键...")
- `body`:正文(500-800 字,步骤型)
- `tags`:5-8 个标签(必含 `#AI教程` `#AI效率` `#办公神器`)
- `steps`:结构化步骤数组,每步含 `desc` + `screenshot_prompt`(供配图)
- `image_prompts`:2-4 张配图 prompt(界面截图风格 + 红框高亮)
- `cross_sell_hint`:评论区导流话术(如"完整模板在主页/评论区")

**正文骨架**
```
1. 痛点钩子(1-2句):打工人的具体场景,如"每周写周报都要纠结..."
2. AI解决方案(1段):用哪个工具、核心prompt是什么
3. 手把手步骤(3-5步):每步1句话+截图说明
4. 结果对比(1段):AI生成 vs 手动写的效率/质量对比
5. 福利引导(1段):评论区扣1送完整模板/PPT版本
```

**配图策略**
- 首图:工具界面截图 + 红框高亮关键操作 + 大字结果
- 2-4图:每步操作截图 / before-after对比
- 用百炼 `bl image` 生成"模拟界面截图"(prompt强调"软件界面截图风格")

### 2.2 Skill 轨 `skill`

**笔记字段**
- `title`:标题(如"我写了个AI skill,自动把会议纪要变PPT大纲")
- `body`:正文(600-900 字,讲设计思路 + 演示)
- `skill_script`:skill 的核心代码/prompt 模板(贴在笔记正文里,小红书接受代码块)
- `demo_input` / `demo_output`:演示输入输出文本
- `tags`:5-8 个(必含 `#AI技能` `#AI自动化` `#自定义skill`)
- `image_prompts`:流程图 + 调用截图

**正文骨架**
```
1. 痛点钩子:重复性工作的痛点(如"每次整理会议纪要都要2小时")
2. skill设计思路:为什么这么设计、核心逻辑1段讲清
3. skill代码/prompt:核心代码块贴出来
4. 调用演示:输入示例 → 输出示例
5. 复用建议:怎么改造成你自己的skill
6. 福利引导:完整skill文件在评论区/主页
```

**配图策略**
- 首图:skill 调用流程图(用 bl image 生成"流程图风格")
- 2-3图:代码截图 / before-after对比 / 输出示例

### 2.3 PPT 种草轨 `ppt`(变现核心)

**笔记字段**
- `title`:标题(如"我用AI做了份季度汇报PPT,老板追着问怎么做的")
- `body`:正文(400-600 字,偏展示型,不要太长影响完播)
- `tags`:5-8 个(必含 `#PPT模板` `#AI做PPT` `#办公神器`)
- `ppt_file`:生成的 PPT 文件路径(`data/ppt_templates/{topic_id}/template.pptx`)
- `image_prompts`:PPT 多页缩略图九宫格
- `cross_sell_hint`:核心导流话术(必填)

**正文骨架**
```
1. 成品展示钩子(1-2句):"这份PPT老板追着我问怎么做..."
2. 生成过程(2段):用什么AI工具、给了什么prompt、几个步骤
3. 多页展示(贴4-6页缩略图):封面/目录/内容页/总结页
4. 模板亮点(1段):为什么值得用(配色/排版/数据可视化)
5. 模板获取(必填):评论区扣1/主页简介/私信自动发
```

**配图策略**
- 首图:4页PPT缩略图九宫格拼贴 + 大字标题
- 2-6图:每页 PPT 单独展示(用 PPT 转 PNG)

**PPT 文件生成**
- 用 python-pptx 生成 PPT 骨架,LLM 产出每页文案
- 或:LLM 产出大纲 → 调用百炼 bl 文本 → 套用预设 PPT 模板 → python-pptx 填充
- 保存到 `data/ppt_templates/{topic_id}/template.pptx`
- 同步用 LibreOffice `soffice --headless --convert-to png` 把每页转成图片

## 3. 数据结构 `produced.json`

```json
{
  "produced_at": "2026-09-10T21:00:00+08:00",
  "posts": [
    {
      "topic_id": "tut_001",
      "track": "tutorial",
      "status": "ready",
      "title": "打工人哭了!AI一键生成Excel周报模板,3秒搞定",
      "body": "...",
      "tags": ["AI教程", "AI效率", "办公神器", "打工人必备", "Excel技巧"],
      "steps": [
        {"desc": "1. 打开AI工具,输入周报prompt",
         "screenshot_prompt": "AI工具界面截图,prompt输入框,红框高亮"}
      ],
      "image_prompts": ["AI工具界面截图风格,红框高亮关键操作"],
      "image_paths": ["data/images/tut_001/01.png"],
      "cross_sell_hint": "完整周报模板已打包进PPT模板包,评论区扣1送",
      "post_time_hint": "12:00-13:00 或 19:00-21:00"
    },
    {
      "topic_id": "skill_001",
      "track": "skill",
      "status": "ready",
      "title": "我写了个AI skill,自动把会议纪要变PPT大纲",
      "body": "...",
      "skill_script": "def meeting_to_ppt_outline(...)...",
      "demo_input": "[会议纪要原文]",
      "demo_output": "[PPT大纲结构]",
      "tags": ["AI技能", "AI自动化", "自定义skill", "AI agent"],
      "image_paths": ["data/images/skill_001/01.png"]
    },
    {
      "topic_id": "ppt_001",
      "track": "ppt",
      "status": "ready",
      "title": "我用AI做了份季度汇报PPT,老板追着问怎么做的",
      "body": "...",
      "tags": ["PPT模板", "AI做PPT", "汇报PPT", "办公神器", "PPT灵感"],
      "ppt_file": "data/ppt_templates/ppt_001/template.pptx",
      "image_paths": [
        "data/ppt_templates/ppt_001/page_01.png",
        "data/ppt_templates/ppt_001/page_02.png"
      ],
      "cross_sell_hint": "完整20页模板评论区扣1,主页简介可获取",
      "post_time_hint": "20:00-22:00"
    }
  ]
}
```

## 4. 实现方案

### 工具栈
- Python 3.11
- LLM:GLM-5.3 或 qwen3.8-max(标题/正文/skill脚本生成)
- 百炼 `bl image`(文生图,配图)
- `python-pptx`(PPT 文件生成)
- `LibreOffice` headless(PPT → PNG)
- 可选:`playwright` 自动化发布(后期才上,先手动发布)

### 执行流程
```
1. 读 judged.json
2. 按 priority 顺序遍历每轨 topics:
   if track == "tutorial":
     a. 生成5个候选标题 → LLM自评 → 选1
     b. 生成步骤数组(每步含 desc + screenshot_prompt)
     c. 按"教程正文骨架"生成 body
     d. 扩展 tags,植入 cross_sell_hint
     e. 调 bl image 生成 2-4 张界面截图风格配图
   if track == "skill":
     a. 生成标题
     b. 生成 skill_script + demo_input/output
     c. 按"skill正文骨架"生成 body
     d. 调 bl image 生成流程图/代码截图风格配图
   if track == "ppt":
     a. 生成标题
     b. LLM 产出 PPT 大纲(每页文案)
     c. python-pptx 套模板生成 pptx 文件
     d. LibreOffice 转 PNG → 多页缩略图
     e. 按"PPT正文骨架"生成 body
     f. 配图直接用 PPT 页面缩略图(九宫格拼贴做首图)
   f. 写入 produced.json
3. 全部产出后,人工/自动发布 → 把笔记 URL 写回 produced.json
```

### 成本控制
- 标题候选 5 选 1:一次 LLM 调用产出 5 个,内部自评
- 正文一次生成,失败重试 1 次
- 教程轨配图允许"界面截图风格模拟",不追求 100% 真实
- PPT 轨配图复用 PPT 页面缩略图,不另生图

## 5. 示例运行(本赛道)

承接 02 的 top 10 选题(教程 5 / skill 3 / PPT 2):

1. **tut_001 AI一键做周报**
   - 标题:"打工人哭了!AI一键生成Excel周报模板,3秒搞定"
   - 正文 600 字 + 4 步截图
   - 评论区导流:"完整周报模板已打包进PPT模板包"

2. **skill_001 会议纪要→PPT大纲 skill**
   - 标题:"我写了个AI skill,自动把会议纪要变PPT大纲"
   - 贴 skill 代码 + 演示输入输出
   - 配图:调用流程图 + before-after

3. **ppt_001 季度汇报PPT模板**
   - 标题:"我用AI做了份季度汇报PPT,老板追着问怎么做的"
   - 生成 20 页 PPT 文件,4-6 页做笔记配图
   - 评论区:"完整20页模板扣1,主页可获取"

4. ... 直到 top 10 全部产出或人工叫停

## 6. 发布与回流

### 发布
- 第一阶段:人工发布,03 把成品打包(标题+正文+标签+图片+PPT文件)交给运营者
- 第二阶段:接入 playwright 自动化登录发布(需谨慎,小红书对自动化敏感)

### 数据回流 → feedback.json
发布后 T+1 / T+3 / T+7 各采一次,**PPT 轨额外记录成单数据**:
```json
{
  "post_url": "https://www.xiaohongshu.com/note/xxx",
  "topic_id": "ppt_001",
  "track": "ppt",
  "keyword": "AI生成汇报PPT",
  "metrics": {
    "views": 25000,
    "likes": 1200,
    "collects": 950,
    "comments": 180,
    "ctr": 0.048,
    "comment_cta_count": 95,
    "ppt_sales": 23,
    "ppt_revenue": 690
  },
  "collected_at": "2026-09-12T21:00:00+08:00"
}
```

01 下一轮读取 feedback.json 时:
- 高 CTR/收藏率关键词加权(教程/skill 轨)
- 高 PPT 成单关键词大幅加权(PPT 轨)
- 低表现关键词降权或拉黑
- 完成"内容生产 → 流量 → 变现"闭环

## 7. 失败与降级

- LLM 生成正文失败 → 跳过该 topic,标 skipped,不阻塞后续
- `bl image` 失败 → 用纯文字笔记 + 系统占位图,标 `image_status: failed` 供运营者人工补图
- python-pptx 生成 PPT 失败 → 该 topic 标 skipped,02 下轮转产纯文字种草文
- 全部 topic 失败 → 写空 produced.json 但带 error 字段,通知 02 调整选题策略

## 8. 合规与账号安全

- 不直接贴外链/二维码到正文(小红书限流)
- 导流话术走评论区首条 + 主页简介
- PPT 模板不在笔记正文里发完整文件,只发预览图,完整文件走私域
- 教程截图避免完整露出他人账号/隐私信息
- skill 代码如涉及 API key,用 `sk-xxx` 占位,不贴真实密钥
